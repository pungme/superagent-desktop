# Superagent 1.9.1-beta.27

- The agent keeps a browser tab of its own. Opening a tab to look something up no longer has it taken over by the agent's next page.
- A project with a simulator attached keeps its own icon; a small phone at the right of the row says the simulator is there.
- The phone follows the Pinned order you set on the Mac, can reorder pins itself, and sees which repos are still cloning (needs the iPhone app update too).
