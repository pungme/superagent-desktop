node_modules
dist
out
.DS_Store
.eslintcache
*.log*

# Playwright e2e output (generated each run)
test-results
playwright-report

# onnxruntime wasm, copied from node_modules by scripts/copy-ort.mjs
src/renderer/public/ort/

# Apple notarization credentials for release builds — never commit
.env

# compiled by native/build.sh
native/simfb

# fetched by scripts/fetch-baguette.mjs (pinned upstream release)
native/baguette
native/baguette.LICENSE

# Incremental build caches — regenerated on every typecheck
*.tsbuildinfo
