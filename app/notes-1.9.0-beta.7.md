# Superagent 1.9.0-beta.7

Beta channel.

- Adding a code folder now opens exactly one usable root conversation, represented by the folder row, without a duplicate **New chat — no branch yet** child.
- Claude Code's **Default** model selection now falls back from Fable to Opus when Fable is unavailable or its allowance is exhausted, including in resumed conversations.
