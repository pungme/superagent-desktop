# Superagent 1.9.1-beta.19

- The model picker now lists the models Claude Code itself offers, read from the installed CLI, so it is current without an app update. It shows Opus 5.5 and Fable 5.1 today, and Default names the model it actually resolves to.
- A chat pinned to a model that has left the list shows that model's name on the pill instead of claiming Default.
