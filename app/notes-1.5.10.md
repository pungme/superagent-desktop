### Fixed
- **The simulator no longer flashes open and vanishes.** It was keyed to the project before the chat finished resolving, then closed itself; it now stays put.
- **Worktree names are readable in the sidebar** without dragging it wider — the branch chip stays pinned and the title gives way instead.
- **Browser pane lines up with its frame** as the layout settles (no more gap under the toolbar / covered "✕ Close" on a PDF).
- **Model "Default"** no longer mislabels itself — it uses your account's model instead of forcing Sonnet.
- One close ✕ on the preview pane instead of two.
- Terminal steps read "Terminal", not "Running", on a finished turn; simulator tool steps read as plain words.

### Calendar
- Responsive when the window is narrow.
